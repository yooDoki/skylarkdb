# 2026-04-13 工作日志

## SkylarkDB UI 深度扁平化重构

- **用户不满意之前的保守优化**，要求更彻底的改动
- **核心改动**：
  1. **完全移除 App Header 行**（之前只缩减了高度），Logo+主题+设置整合进 ConnectionList 侧边栏顶部
  2. **App.tsx 从 flex-col 改为 flex 布局**，内容区从第一行像素开始，无任何顶部浪费
  3. **MySQLExplorer 全面扁平化**：移除所有 Card/CardHeader/gradient/shadow-card/backdrop-blur，改为原生 border-r 分栏 + 纯文本工具栏
  4. **RedisExplorer 全面扁平化**：同上，4个信息卡片改为单行文字统计，键列表和详情区改为原生分栏
  5. **欢迎页精简**：去掉 blur 光晕/Sparkles/双数据库图标，只留单图标+一行提示
  6. **ConnectionItem 精简**：rounded-xl→rounded-md，间距/图标/字体全面缩小
  7. **侧边栏宽度**：w-56 (224px)，MySQL 表列表 w-60 (240px)，Redis 键列表 w-60 (240px)
- **文件**：App.tsx、ConnectionList.tsx、ConnectionItem.tsx、MySQLExplorer.tsx、RedisExplorer.tsx
- **构建验证**：通过，零 lint 错误

## ConnectionItem 布局优化

- **问题**：侧边栏 w-56 太窄，连接名被状态灯+图标背景+类型标签挤得看不到
- **修复**：
  1. 去掉图标背景包裹（p-1.5 rounded-md bg-*），改为纯图标
  2. 状态灯合并到图标右下角，不再独占位置
  3. 连接名独占第一行，移除同行类型标签
  4. 只读标签移到地址行
  5. 移除独立编辑按钮（通过更多菜单操作）
  6. 操作按钮从 h-6 缩到 h-5
- **文件**：ConnectionItem.tsx

## 添加行按钮文字补全

- **问题**：数据工具栏的 + 按钮缺少"行"文字，旁边的 + 列有文字
- **修复**：给添加行按钮补充 `<span>行</span>`
- **文件**：MySQLExplorer.tsx

## v0.2.0 正式版发布

- 统一版本号：Cargo.toml 从 0.1.8 → 0.2.0
- 提交并推送 tag v0.2.0

## SkylarkDB UI 优化（第一轮，已被第二轮覆盖）

- **问题**：Header 区域（TitleBar + App Header 两层叠加）视觉冗余，顶部占用约 92px
- **优化**：
  1. App.tsx Header：高度从 h-14 (56px) 缩减到 h-12 (48px)，移除光晕效果和副标题
  2. 左侧边栏：宽度从 w-80 (280px) 缩减到 w-60 (240px)，折叠按钮位置和样式相应调整
  3. 删除 TitleBar.tsx（未使用）及 index.css 中相关样式
- **文件**：`skylarkdb/src/App.tsx`、`skylarkdb/src/index.css`
- **状态**：构建验证通过

## 导入数据按钮位置调整

- **问题**：「导入数据」按钮出现在每张表的数据视图工具栏，误导用户认为是表级操作。
- **修复**：将该按钮从表数据视图工具栏移除，改为放在侧边栏表列表的工具栏（与「创建表」「刷新」并列），语义上明确为数据库级别操作。
- **文件**：`skylarkdb/src/components/MySQLExplorer.tsx`
